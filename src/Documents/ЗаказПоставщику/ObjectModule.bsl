
Процедура ПередЗаписью(Отказ, РежимЗаписи, РежимПроведения)
	Если Константы.НесколькоВалют.Получить() Тогда
		Если  Валюта.Ссылка = Справочники.Валюта.ПустаяСсылка()  Тогда
			  Валюта = Константы.ВалютаПоУмолчанию.Получить();
		  КонецЕсли;
	  Иначе
		  Валюта = Константы.ВалютаПоУмолчанию.Получить();
	КонецЕсли;
	СуммаЗаказа = Товары.Итог("Стоимость");
КонецПроцедуры
